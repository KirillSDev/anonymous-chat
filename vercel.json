{
    "version": 2,
    "buildCommand": "yarn --prefix client && yarn && yarn build && yarn build:client",
    "builds": [
      { "src": "server/dist/server.js", "use": "@vercel/node" }

    ],
    "routes": [
      { "src": "/(.*)", "dest": "server/dist/server.js" }
    ]
  }