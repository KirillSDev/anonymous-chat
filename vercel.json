{
    "buildCommand": "yarn build && yarn client:build",
    "installCommand": "yarn install && cd client && yarn install & cd ..",
    "routes": [
        {
          "src": "/.*",
          "dest": "server/server.js"
        }
      ]
}